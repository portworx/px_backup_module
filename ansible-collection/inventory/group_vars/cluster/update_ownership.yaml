---
# Clusters to update
backup_share_updates:
  - name: "cluster-1"
    uid: "cluster-1-uid"
    backup_share:
      add:
        groups:
          - id: "group1"
            access: "FullAccess"
        collaborators:
          - id: "user2"
            access: "View"
      delete: 
        groups: []
        collaborators: []

# Global update configuration
validate_certs: false